# Plantilla de variables de entorno. Este archivo SÍ se versiona en git.
# Copialo como `.env` y completá los valores reales:
#
#   Windows (PowerShell):  Copy-Item .env.example .env
#   macOS / Linux:         cp .env.example .env

# Cadena de conexión a PostgreSQL.
# Formato: postgresql://USUARIO:CONTRASEÑA@HOST:PUERTO/NOMBRE_DE_LA_BASE?schema=public
DATABASE_URL=
